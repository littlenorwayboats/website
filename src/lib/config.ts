/**
 * Public site configuration. Safe to commit — no secrets belong here.
 *
 * Optional local overrides go in `.env.local` (gitignored). See `.env.example`.
 */
export const publicConfig = {
  siteUrl: "https://littlenorwayboats.com",
} as const;
